<section class="row">
        <div class="card-body">
            <h2 style="display: inline-block;">Mascotas de {{username}}</h2>
            <a href="/pets/mypets/add/{{_id}}" class="btn btn-success">Añadir mascota externa</a>
        </div>
    {{#each pets }}
    {{#ifEquals owner_id ../userId}}
    <div class="col-sm-6">
        <div class="card">
            <div class="card-body">
                <h4>
                    {{name}} <a href="/pets/mypets/edit/{{_id}}"><i class="fas fa-edit"></i></a>
                </h4>
                <p>
                    {{race}}
                </p>
                <a href="/pets/mypets/leave/{{_id}}" class="btn btn-warning">Abandonar</a>
                <a href="/pets/mypets/sacrifice/{{_id}}" class="btn btn-danger">Sacrificar</a>
            </div>
        </div>
    </div>
    {{/ifEquals}}
    {{/each}}
</section>